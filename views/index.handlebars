<h1>burgers!</h1>

<h2>burgers that are not burgery!</h2>

<ul>
  {{#each burgers}}
    {{#unless burgery}}
      {{> burgers/burger-block burger=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are burgery!</h2>

<ul>
  {{#each burgers}}
    {{#if burger}}
      {{> burgers/burger-block burger=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="burgery">burgery?</label><br>
    <input type="radio" name="burgery" value="1" checked> burgery!<br>
    <input type="radio" name="burgery" value="0"> Awake
  </div>

  <button type="submit">Add burger</button>
</form>
